# Local: usa una DB de Railway o PostgreSQL local
# Ejemplo local con Docker: postgresql://postgres:password@localhost:5432/restaurante
# En Railway: se llena automáticamente al agregar el plugin de PostgreSQL
DATABASE_URL="postgresql://postgres:password@localhost:5432/restaurante"
PORT=3001
NODE_ENV=development
